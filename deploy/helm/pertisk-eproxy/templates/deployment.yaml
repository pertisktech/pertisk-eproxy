apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pertisk-eproxy.fullname" . }}
  labels:
    {{- include "pertisk-eproxy.labels" . | nindent 4 }}
spec:
  {{- if .Values.autoscaling.enabled }}
  replicas: {{ .Values.autoscaling.minReplicas }}
  {{- else }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "pertisk-eproxy.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "pertisk-eproxy.selectorLabels" . | nindent 8 }}
      {{- if .Values.controller.configMap.enabled }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "pertisk-eproxy.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          image: {{ include "pertisk-eproxy.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.controller.config.http_port }}
              protocol: TCP
            - name: https
              containerPort: {{ .Values.controller.config.https_port }}
              protocol: TCP
            - name: management
              containerPort: {{ .Values.controller.config.management_port }}
              protocol: TCP
            {{- if .Values.service.http3Port }}
            - name: http3
              containerPort: {{ .Values.controller.config.https_port }}
              protocol: UDP
            {{- end }}
          env:
            - name: PERTISK_VERSION
              value: {{ .Values.image.tag | default .Chart.AppVersion | quote }}
            - name: PERTISK_MODE
              value: ingress
            - name: PERTISK_K8S_INGRESS_ENABLED
              value: {{ .Values.ingress.enabled | quote }}
            - name: PERTISK_K8S_INGRESS_CLASS
              value: {{ include "pertisk-eproxy.ingressClassName" . | quote }}
            {{- if .Values.ingress.watchNamespace }}
            - name: PERTISK_K8S_NAMESPACE
              value: {{ .Values.ingress.watchNamespace | quote }}
            {{- end }}
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: PERTISK_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: PERTISK_K8S_LEADER_ELECTION_ENABLED
              value: {{ .Values.leaderElection.enabled | quote }}
            - name: PERTISK_K8S_LEADER_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: PERTISK_K8S_LEADER_NAME
              value: {{ default (printf "%s-leader" (include "pertisk-eproxy.fullname" .)) .Values.leaderElection.leaseName | quote }}
            - name: PERTISK_K8S_LEADER_LEASE_DURATION_SECONDS
              value: {{ .Values.leaderElection.leaseDurationSeconds | quote }}
            - name: PERTISK_K8S_LEADER_RENEW_INTERVAL_SECONDS
              value: {{ .Values.leaderElection.renewIntervalSeconds | quote }}
            - name: PERTISK_HELM_ENABLED
              value: {{ .Values.helm.enabled | quote }}
            - name: PERTISK_HELM_RELEASE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['app.kubernetes.io/instance']
            - name: PERTISK_HELM_NAMESPACE
              value: {{ default .Release.Namespace .Values.helm.namespace | quote }}
            - name: PERTISK_HELM_HISTORY_MAX
              value: {{ .Values.helm.historyMax | quote }}
            - name: PERTISK_K8S_TLS_DIR
              value: {{ .Values.persistence.mountPath }}/k8s-tls
            - name: PERTISK_K8S_CONTROLLER_NAME
              value: {{ include "pertisk-eproxy.fullname" . | quote }}
            - name: PERTISK_K8S_POD_LABEL_SELECTOR
              value: {{ printf "app.kubernetes.io/name=%s,app.kubernetes.io/instance=%s" (include "pertisk-eproxy.name" .) .Release.Name | quote }}
            {{- if .Values.controller.configMap.enabled }}
            - name: PERTISK_CONFIG_FILE
              value: /opt/pertisk_eproxy/config/ingress.json
            {{- end }}
            {{- if .Values.auth.enabled }}
            - name: PERTISK_AUTH_MODE
              value: {{ .Values.auth.mode | quote }}
            - name: PERTISK_ADMIN
              valueFrom:
                secretKeyRef:
                  name: {{ include "pertisk-eproxy.authSecretName" . }}
                  key: {{ .Values.auth.usernameKey | quote }}
            - name: PERTISK_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pertisk-eproxy.authSecretName" . }}
                  key: {{ .Values.auth.passwordKey | quote }}
            - name: PERTISK_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "pertisk-eproxy.authSecretName" . }}
                  key: {{ .Values.auth.apiTokenKey | quote }}
                  optional: true
            - name: PERTISK_AUTH_SIGNING_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "pertisk-eproxy.authSecretName" . }}
                  key: {{ .Values.auth.signingSecretKey | quote }}
                  optional: true
            {{- end }}
            {{- if and .Values.auth.enabled .Values.auth0.domain }}
            - name: PERTISK_AUTH0_DOMAIN
              valueFrom:
                secretKeyRef:
                  name: {{ include "pertisk-eproxy.authSecretName" . }}
                  key: {{ .Values.auth0.domainKey | quote }}
                  optional: true
            - name: PERTISK_AUTH0_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "pertisk-eproxy.authSecretName" . }}
                  key: {{ .Values.auth0.clientIdKey | quote }}
                  optional: true
            - name: PERTISK_AUTH0_AUDIENCE
              valueFrom:
                secretKeyRef:
                  name: {{ include "pertisk-eproxy.authSecretName" . }}
                  key: {{ .Values.auth0.audienceKey | quote }}
                  optional: true
            {{- else if .Values.auth0.domain }}
            - name: PERTISK_AUTH0_DOMAIN
              value: {{ .Values.auth0.domain | quote }}
            - name: PERTISK_AUTH0_CLIENT_ID
              value: {{ .Values.auth0.clientId | quote }}
            - name: PERTISK_AUTH0_AUDIENCE
              value: {{ .Values.auth0.audience | quote }}
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            {{- if .Values.controller.configMap.enabled }}
            - name: config
              mountPath: /opt/pertisk_eproxy/config/ingress.json
              subPath: ingress.json
              readOnly: true
            {{- end }}
            {{- if .Values.persistence.enabled }}
            - name: data
              mountPath: {{ .Values.persistence.mountPath }}
            {{- end }}
          {{- with .Values.startupProbe }}
          startupProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        {{- if .Values.controller.configMap.enabled }}
        - name: config
          configMap:
            name: {{ include "pertisk-eproxy.fullname" . }}-config
        {{- end }}
        {{- if .Values.persistence.enabled }}
        - name: data
          {{- if .Values.persistence.storageClass }}
          persistentVolumeClaim:
            claimName: {{ include "pertisk-eproxy.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
